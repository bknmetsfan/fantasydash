Drop FantasyPros rest-of-season exports (`*.csv`) here. When any CSV is
present it drives the endgame tier (QB12 / RB10 / WR15 / TE5) instead of
Sleeper's remaining weekly projections. Replace the file each week rather
than adding a new one: every CSV in this folder is merged.
